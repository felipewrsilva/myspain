# Agent core (this repo)

Per-project continuity lives in `.agent/CONTINUITY.md`.

Global always-apply agent rules live in `~/.cursor/rules/agent-*.mdc` (accuracy, safety, containers, continuity, workflow, definition of done).

To (re)bootstrap this layout, use the personal skill `agent-core-bootstrap`.
